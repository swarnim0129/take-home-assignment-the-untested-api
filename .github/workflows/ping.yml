name: Ping Render API

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API to prevent idle
        run: curl -s --connect-timeout 10 https://task-api-f2tc.onrender.com/tasks > /dev/null || echo "Ping failed"